{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "fe": "lerna run --scope react-frontend start",
    "be": "lerna run --scope express-backend start",
    "both": "lerna run --parallel start"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "3.4.0"
  }
}
